<%= form_for(current_user.userevents.build(attended_event_id: @event.id)) do |f| %>
  <div>
  	<%= f.hidden_field :attended_event_id %>
  </div>
  <%= f.submit "Attend", class: "btn btn-large btn-primary" %>
<% end %>